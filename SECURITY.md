# 安全策略

## 支持版本

当前仅支持最新发布版本。`v0.0001` 是第一个公开版本。

## 报告安全问题

请不要在公开 Issue 中发布密码、密钥、真实客户数据、数据库备份或可利用漏洞的完整细节。请通过项目维护者配置的私密安全渠道报告，并提供：

- 受影响的版本和部署方式（EXE 或 Docker）；
- 复现步骤、影响范围和必要的日志片段；
- 已知的临时缓解措施。

在公开仓库配置私密联系方式前，请暂时使用 GitHub Security Advisories 的私密报告入口。

## 数据处理要求

`.env`、`config.json`、`data/`、上传文件、数据库备份、内部文档以及 `deploy/docker/secrets/*.txt` 不得提交到公开仓库。演示数据只能使用占位账号、虚构地址和不可用的示例密钥。

若 Docker Secret、数据库密码、JWT 密钥或管理员密码疑似泄露，请立即更换相应凭据；管理员密码恢复操作见 [管理员账号与密码](docs/管理员账号与密码.md)。
