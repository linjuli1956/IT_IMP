# 第三方许可证说明

本项目自身使用 Apache-2.0，第三方依赖不因本项目许可证而改变其原有许可证。

主要组件包括：

- Nuxt、Vue、Vite 与 Element Plus：以各自仓库和 npm 包中附带的许可证为准；
- Prisma：以其发行包附带的许可证为准；
- Wails、Go modules：以对应模块仓库和发行包附带的许可证为准；
- Node.js、Bun、MySQL 和基础 Docker 镜像：以各自发行版及镜像清单中的许可证为准。

发布二进制或 Docker 镜像时，应保留上游组件的版权和许可证声明，并按实际锁定版本生成依赖清单。此文件不是对第三方许可证文本的替代。
