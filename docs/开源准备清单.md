# 开源准备清单

> 本文档用于在创建公开 GitHub 仓库前逐项检查，确保不泄露任何内部数据。
> 最后更新：2026-07-25

---

## ⚠️ 重要提醒

**当前内部 Git 历史包含以下敏感信息，不能直接用于公开仓库：**

1. **真实门店名称**：内部门店名称（composables + seed.ts）
2. **真实供应商名称和电话**：内部供应商资料（seed.ts）
3. **真实手机号码**：内部联系方式（seed.ts 计提模板）
4. **真实商户号和密钥**：支付商户号、API 密钥等（seed.ts 支付配置）
5. **真实服务器 IP 和密码**：内部服务器地址和密码（seed.ts 服务器数据）
6. **真实域名和密码**：内部域名和密码（seed.ts 域名数据）
7. **MySQL 服务器 IP**：内部数据库地址（docs/开发计划文档.md）

**结论：必须创建全新的干净 Git 仓库，不能沿用旧历史。**

**⚠️ 如果 MySQL root 密码曾出现在 Git 历史中（经检查未发现），需要先更换密码。**
当前检查结果：Git 历史中未发现明文 MySQL 密码，密码通过 DPAPI 加密保存在本机 config.json（已 .gitignore）。
但仍建议在开源前更换 MySQL root 密码作为最佳实践。

---

## 检查清单

### 1. Git 历史

- [ ] **不沿用旧 Git 历史** — 创建全新仓库，`git init` 后一次性提交所有清理后的文件
- [ ] 使用旧仓库审计工具确认历史中不存在内部 IP、商户号、密钥或密码
- [ ] 旧仓库标记为 `internal-only`，永不推送到 GitHub

### 2. 数据分层

| 层级 | 文件 | 说明 | 公开仓库 |
|------|------|------|----------|
| 演示数据 | `web/prisma/seed.demo.ts` | 虚构门店/供应商/服务器，所有密码为 `DEMO_PASSWORD_PLACEHOLDER` | ✅ 包含 |
| 内部数据 | `web/prisma/seed.ts` | 真实门店/供应商/服务器/支付配置 | ❌ .gitignore 排除 |
| 内部文档 | `docs/副本*.xlsx` | 真实预算 Excel | ❌ .gitignore 排除 |
| 配置 | `.env` / `config.json` | 数据库地址/密码 | ❌ .gitignore 排除 |
| 示例配置 | `.env.example` | 占位符，无真实值 | ✅ 包含 |

- [ ] 确认 `seed.ts` 已在 `.gitignore` 中
- [ ] 确认 `seed.demo.ts` 不在 `.gitignore` 中（`!web/prisma/seed.demo.ts`）
- [ ] 确认 `prisma.config.ts` 支持 `SEED_MODE=demo` 环境变量切换
- [ ] 确认 `.env.example` 中无真实 IP、密码、用户名

### 3. 代码扫描

- [ ] 全局搜索内部 IP、电话、商户号、密钥和密码 — 确认不在任何已提交文件中
- [ ] 全局搜索 `taixing` — 评估是否需要替换（项目名称 "泰兴" 可保留，但具体地址/电话需移除）
- [ ] 全局搜索 `192.168.1.` — 确认只在演示数据中以 `192.168.0.` 形式出现
- [ ] composables 中的真实数据需替换为演示数据（后端完成后统一清理）

### 4. 构建产物

- [ ] `desktop/build/bin/` 不在仓库中（.gitignore 已排除）
- [ ] `*.exe` 不在仓库中（.gitignore 已排除）
- [ ] `web/.output/` 不在仓库中（.gitignore 已排除）
- [ ] `web/.nuxt/` 不在仓库中（.gitignore 已排除）
- [ ] `node_modules/` 不在仓库中（.gitignore 已排除）

### 5. 开源许可证与文档

- [ ] **LICENSE** — 创建 Apache-2.0 许可证文件
- [ ] **README.md** — 项目介绍、功能列表、安装步骤、技术栈、截图
- [ ] **SECURITY.md** — 安全报告流程、敏感数据处理说明
- [ ] **CONTRIBUTING.md** — 贡献指南、代码规范、提交规范
- [ ] **THIRD_PARTY_LICENSES.md** — 第三方依赖许可证说明（Nuxt/Vue/Element Plus/Prisma/Go/Wails 等）
- [ ] **.env.example** — 已有，确认无真实值

### 6. 文档清理

- [ ] `docs/开发计划文档.md` — 移除内部 IP、密码和真实业务资料
- [ ] `docs/数据库字典.md` — 确认无真实业务数据
- [ ] `docs/项目立项文档.md` — 评估是否包含敏感信息
- [ ] `docs/副本*.xlsx` — 已 .gitignore 排除

### 7. 发布前最终验证

- [ ] 克隆新仓库到全新目录
- [ ] 执行 `SEED_MODE=demo npx prisma migrate deploy && SEED_MODE=demo npx prisma db seed`
- [ ] 确认演示数据写入成功
- [ ] 用 `SHOW CREATE TABLE` 确认中文注释存在
- [ ] 全局搜索确认无真实数据残留

---

## 公开仓库目录结构（预期）

```
it-imp-public/
├── .gitignore                    # 完善的 .gitignore
├── .env.example                  # 占位符配置示例
├── LICENSE                       # Apache-2.0
├── README.md                     # 项目介绍
├── SECURITY.md                   # 安全报告流程
├── CONTRIBUTING.md               # 贡献指南
├── THIRD_PARTY_LICENSES.md       # 第三方许可证
├── docs/
│   ├── 数据库字典.md              # 数据库字典
│   ├── 开发计划文档.md            # 开发计划（清理后）
│   ├── 开源准备清单.md            # 本文档
│   └── 前端规划文档.md            # 前端规范
├── web/
│   ├── nuxt.config.ts
│   ├── package.json
│   ├── prisma/
│   │   ├── schema.prisma         # Prisma 数据模型
│   │   ├── seed.demo.ts          # 演示数据（可公开）
│   │   ├── prisma.config.ts      # Prisma 配置
│   │   └── migrations/
│   │       └── 20260725000000_init/
│   │           └── migration.sql # 初始迁移（含中文注释）
│   ├── app/                      # Nuxt 前端源码
│   └── server/                   # Nuxt 后端源码
├── desktop/
│   ├── app.go                    # Go 后端
│   ├── main.go
│   ├── go.mod
│   ├── wails.json
│   └── frontend/                 # Wails 前端
└── data/
    ├── .gitkeep
    ├── uploads/.gitkeep
    └── logs/.gitkeep
```

**注意**：`web/prisma/seed.ts`（内部真实数据）不在公开仓库中。

---

## 长期规范（从现在起执行）

1. **不在代码中硬编码真实数据** — 所有真实门店名、IP、密码、商户号等只能存在于 `seed.ts`（已 .gitignore）
2. **新功能使用演示数据** — composables 和 API 默认使用虚拟数据，真实数据通过 seed.ts 注入
3. **文档不引用真实地址** — 文档中使用 `REMOTE_HOST`、`DEMO_PASSWORD` 等占位符
4. **日志不输出密码** — `sanitizeLog()` 已实现，持续维护
5. **配置通过环境变量** — 数据库连接、JWT 密钥等通过 `.env` 或 EXE 注入，不硬编码
